# Task 2: Add MIT License File

## Goal

Add the actual MIT license text to the repository. The `pyproject.toml` already declares `license = {text = "MIT"}` but there is no `LICENSE` file in the repo.

## Requirements

1. Create a `LICENSE` file at the repo root with the standard MIT license text.
2. Use the year **2025** and copyright holder **Sagi Hirshfeld** (the git user and GitHub owner of `sagihirshfeld/must-gather-downloader`).
3. That's it — this is a small, self-contained task.

## Commit

Create a single commit: "Add MIT LICENSE file".
